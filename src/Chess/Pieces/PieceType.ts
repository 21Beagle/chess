export default class PieceType {
    name: string;
    id: string;
    value: number;
    constructor(name: string, id: string, value: number) {
        this.name = name;
        this.id = id;
        this.value = value;
    }
}
